[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gitcad-core"
version = "0.2.0"
description = "gitcad core: canonical text, stable identity, the Part standard, report pipeline"
readme = "../../README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/gitcad"]
